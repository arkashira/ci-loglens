# Log Ingestion
A simple log ingestion system that allows uploading log files and searching for specific messages.

## Usage
1. Create an instance of the `LogIngestion` class.
2. Upload a log file using the `upload_log` method.
3. Query a log by its job ID using the `query_log` method.
4. Search for logs containing a specific message using the `search_logs` method.
